# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# ---------------------------------------------------------------
# Brota — este repo es PÚBLICO. Lo de abajo no sube nunca.
# ---------------------------------------------------------------

# Material de ingesta del cliente: brief con precios, condiciones
# comerciales y extracciones del sitio anterior. Vive en local y
# en el vault, jamás en GitHub.
/material/

# Ajustes locales de Claude Code (permisos de esta máquina)
.claude/settings.local.json

# Salidas de Playwright (los screenshots de QA sí se versionan,
# en docs/qa/ — esto es el ruido del runner)
/test-results/
/playwright-report/
/blob-report/
/.playwright/
